{
  "name": "projet-graphql",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "contracts": "yarn workspace @micro-project/contracts",
    "billing": "yarn workspace @micro-project/billing",
    "build": "lerna run build --stream",
    "clean": "rm ./packages/*/*.tsbuildinfo"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.1.8",
    "typescript": "^4.7.4"
  }
}
